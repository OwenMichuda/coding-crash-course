name = "Owen Michuda"
age = 24

# Note that we have to convert the age to a string when concatenating it with the name, 
# because Python does not allow concatenation of strings and integers directly.
# We will go over data types next.
print("My name is " + name + " and I am " + str(age) + " years old.")

# Now define the following:
full_name = "Charlotte Giff"

# Assign your age as an integer
age = 23

# Assign your height in meters as a float (e.g., 1.75)
height = 1.5

# Assign whether you are right or not as a boolean
is_right = False